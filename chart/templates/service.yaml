apiVersion: v1
kind: Service
metadata:
  name: mcp-glimmung
  namespace: mcp-glimmung
spec:
  type: ClusterIP
  selector:
    app: mcp-glimmung
  ports:
    - name: http
      port: 80
      targetPort: proxy
